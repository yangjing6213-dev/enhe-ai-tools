# RED / GREEN 证据

## 历史 RED 保留

| 阶段 | 已验证事实 | 状态 |
| --- | --- | --- |
| R | 320px 精选 CTA 修复前交叉 7.796875px；52px reserve 后间距 44.203125px；卡宽与 rail scrollWidth 不变 | `BLOCKED` 历史保留 |
| R2 | 精选自然安全宽 419px，固定区最大 420px，421px 起零布局回归；最小间距 8.375px；全部产品在 320px 交叉 19px | `BLOCKED` 历史保留 |
| R3 | 全部产品 320—480px 双语双轮 644/644 失败，间距 -19px；不存在自然安全断点 | `BLOCKED` 历史保留 |

## R4 结构性 RED

- `UNIFIED_EXCLUSION_RED_RUNS=3`
- `UNIFIED_EXCLUSION_RED_FAILED=3`
- `ALL_PRODUCTS_RED_INTERSECTION_PX=19`
- 精选当前独立安全区仍给出约 44.203125px 间距。
- RED 时入口仍为 44×44，产品卡宽、grid、rail 未被其他改动污染。

这证明失败来自结构合同，而不是偶发截图、数据顺序或单次浏览器误差。

## 源级 TDD

1. 先加入组件作用域合同断言，测试为 4/5 通过、1 个预期失败。
2. 再加入 484px 生产模式边界断言，测试仍为 4/5 通过、1 个预期失败。
3. 实现 token、明确作用域与断点后，同一组为 5/5 通过。
4. Playwright 对 483/484/768 三个边界的定向检查为 1/1 通过。

## 阈值证据纠正

首个单轮阈值扫描未真正强制文字按钮，不能证明文字模式安全，被明确拒绝。修正 helper，使入口保持文字形态并强制 104px reserve 后重新执行两轮：46,080 条、失败 0、二维碰撞 0、最小相关间距 12.453125px、首个安全宽度 481px。最终源码 standalone 又以同样条件生成 `03-TEXT-MODE-SAFE-WIDTH-SCAN.csv`，结果一致。

## 审查反馈闭环

第一轮审查发现分数断点、空覆盖 fail-open、dialog 焦点逃逸、分类层叠与 safe-area reserve；第二轮发现首页品牌 CTA 漏项、FAQ/菜单焦点返回和压力覆盖 fail-open。对应测试先失败，再以最小生产改动修复。取消的旧矩阵没有作为最终证据；最终文档只引用修复后的 v4/v5 文件。

