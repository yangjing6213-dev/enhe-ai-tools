# Core Contract Evidence

`CORE_CONTRACT_USES_CHILD_PROCESS=NO`

`CORE_CONTRACT_USES_SOCKET=NO`

`CORE_CONTRACT_USES_REAL_STATE_FILE=NO`

`CORE_CONTRACT_USES_FAKE_CLOCK=NOT_RUN`

`CORE_CONTRACT_STRESS_RUNS=100`

`CORE_CONTRACT_STRESS_PASSED=NOT_RUN`

The core contract suite was not created because the real production state writer failed its mandatory independent gate. No core GREEN claim is made.
